package com.xenon.store.viewmodel // Or your appropriate package

import androidx.lifecycle.ViewModel

class StoreViewModel : ViewModel() {
    // You can add LiveData or StateFlow properties here later
    // to hold and manage UI-related data.
    // For now, it's empty to just make the app build.
}